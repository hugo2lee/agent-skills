# Vertical Slices

Choose a slice that proves a real path end to end, even if the path is narrow. A slice should exercise the behavior that matters rather than creating empty layers for future use.

After each slice, record what is now supported, which old path remains, and what verification protects the transition. Use the result to choose the next slice instead of following a speculative layer checklist.
