interface:
  display_name: "Code Review and Quality"
  default_prompt: "Use $code-review-and-quality to review this change for behavior, boundaries, regressions, maintainability, and verification evidence."
  short_description: "Find defects with evidence"
policy:
  allow_implicit_invocation: true
