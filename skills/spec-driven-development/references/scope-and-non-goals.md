# Scope and Non-Goals

Write what the change does not attempt. Non-goals protect the task from opportunistic cleanup, unrelated refactoring, and architecture expansion.

When a possible improvement is discovered, classify it as:

- required for the requested behavior;
- required for safety or compatibility;
- useful but independent;
- speculative.

Only the first two belong in the current implementation unless the user explicitly expands scope.
