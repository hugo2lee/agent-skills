# Hypothesis-Driven Debugging

Write competing explanations rather than committing to the first plausible cause.

For each hypothesis state:

- what observation it predicts;
- what experiment can distinguish it;
- what result would falsify it;
- how much the experiment changes the system.

Prefer minimal, reversible experiments. Check the earliest incorrect state and boundary crossing instead of only patching the final error message.
