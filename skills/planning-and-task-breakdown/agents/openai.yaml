interface:
  display_name: "Planning and Task Breakdown"
  default_prompt: "Use $planning-and-task-breakdown to order bounded tasks, dependencies, risks, checkpoints, and completion criteria."
  short_description: "Break work into verifiable slices"
policy:
  allow_implicit_invocation: true
